import React from "react";
export default function App(){
  return (
    <div style={{fontFamily:'system-ui, Segoe UI, Arial', padding:16}}>
      <h1>Red Squirrel — Job Sheet</h1>
      <p>This repo is ready to upload to GitHub to build a Windows installer for free (via GitHub Actions).</p>
      <p>Logo is embedded and used for the installer and desktop shortcut.</p>
    </div>
  );
}
